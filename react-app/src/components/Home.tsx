// suggestions to sign up login if not?
// ie: Login/SignUp to start tweeting!

import PageWrapper from './PageWrapper';

const Home = () => {
  return (
    <PageWrapper>
      <div>home</div>
    </PageWrapper>
  );
};

export default Home;
