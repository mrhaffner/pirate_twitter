const SearchBar = () => {
  // needs to control dropdown
  return (
    <div>
      <div>icon</div>
      <input />
      <button>close button hidden</button>
    </div>
  );
};

export default SearchBar;
